############################################
#inventario recursos humanos
############################################
# Construya una clase ResourceInventory para gestionar recursos urbanos (e.g., bancos,
# farolas). La clase debe incluir atributos para el ID del inventario (cadena) y una lista
# privada de recursos (cadenas). Implemente métodos add_resource(item) para agregar
# un recurso y resource_frequency() para contar la frecuencia de un recurso específico.
# Desarrolle una función resource_priority(count) que calcule una prioridad usando
# math.log(). Acepte el ID y recursos vía input(), imprima la lista y la prioridad, y
# estandarice los recursos con replace().

class ResourceInventory:
    def __init__(self, id_inventory):
    # 
    self.id_inventario = id_inventario # atributo público
    self._recursos = [] # Lista privada

    def add_resource(self, item)
        return
    
    def resource_frequency()
     